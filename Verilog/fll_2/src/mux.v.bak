module mux
   #(parameter N=8)
   (
    input sel,
    input reg[N-1:0] in0, in1,
    output [N-1:0] out
   );
//assign out = sel[1] ? (sel[0] ? d : c) : (sel[0] ? b : a);
assign out = sel? in1 : in0;
endmodule
